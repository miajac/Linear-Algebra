# -*- coding: utf-8 -*-
"""LeastSqures1

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/14uaj8WgE6PQm3J6sy_aHJnr8xaMFNa3N

#**Lab 6 - Least squares I**

Enter your code in the spaces provided. Do not change any of the variable names or function names that are already provided for you. In places where we specify the name of the return value, make sure that your code produces the a value with the correct name.
"""

# Do not edit this cell.

LabID="Lab6"

try:
  from graderHelp import ISGRADEPLOT
except ImportError:
  ISGRADEPLOT = True

"""**Enter your name, section number, and BYU NetID**"""

# Enter your first and last names in between the quotation marks.

first_name="Miaja"

last_name="Coombs"

# Enter your Math 215 section number in between the quotation marks.

section_number="001"

# Enter your BYU NetID in between the quotation marks.  NOT YOUR BYU ID NUMBER!

BYUNetID="miajac"

"""**Import NumPy**"""

import numpy as np

"""**Problem 1**"""

# Replace the values of 0 with the NumPy arrays from Problem 1.

X1 = np.column_stack((np.ones(10), np.arange(5, 55, 5)))
Y1=np.array([3.33,4.43,4.39,5.23,5.67,6.06,7.01,7.16,8.03,8.78])

"""**Problem 2**"""

# Replace the values of 0 with the normal equation coefficient matrix and normal equation right-hand side respectively from Problem 2.
transX=np.transpose(X1)
normal_coef1=np.matmul(transX,X1)
normal_vect1=np.matmul(transX,Y1)

"""**Problem 3**"""

# Replace the value of 0 with the least squares solution beta1 you found in Problem 3.
beta1=np.linalg.solve(normal_coef1,normal_vect1)
print(beta1)

# Define a function whose graph is the line of best fit.
def ls1_line(x):
  v0, a = beta1
  y = a * x + v0
  return y

import matplotlib.pyplot as plt

def create_plots1():
  x_values = np.arange(0, 60, 1)
  y_values = [ls1_line(x) for x in x_values]
  plt.scatter(X1[:, 1], Y1, label='Data Points', color='red')
  plt.plot(x_values, y_values, label='Fitted Line', color='blue')
  plt.grid()
  plt.show()
  return None
create_plots1()

# Replace the value of 0 with your prediction of the satellite's velocity at t=60.

pred1=9.70533333

"""**Problem 4**"""

# Replace the values of 0 with the NumPy arrays from Problem 4.
column1 = np.arange(5, 55, 5)
column2 = column1 ** 2
X2=np.column_stack((column1, column2))

Y2=np.array([20.57,87.48,197.45,347.67,546.12,784.35,1066.02,1390.97,1761.85,2177.34])
print(Y2)

"""**Problem 5**"""

# Replace the values of 0 with the normal equation coefficient matrix and normal equation right-hand side, and least squares solution from Problem 5.
transX2=np.transpose(X2)
normal_coef2=np.matmul(transX2,X2)

normal_vect2=np.matmul(transX2,Y2)

beta2=np.linalg.solve(normal_coef2,normal_vect2)
print(beta2)

"""**Problem 6**"""

# Define a function whose graph is the parabola of best fit.

def ls2_par(x):
  v0 = beta2[0]
  a = beta2[1]
  y = v0 * x + (a) * x ** 2
  return y

# Construct your plot of ls2_par and the corresponding data points here. Put all of your code to create the plots inside the function below.

def create_plots2():
  x_values = np.arange(5, 55, 5)
  y_values = np.array([20.57, 87.48, 197.45, 347.67, 546.12, 784.35, 1066.02, 1390.97, 1761.85, 2177.34])
  y_parabola = [ls2_par(x) for x in x_values]
  plt.scatter(x_values, y_values, label='Data Points', color='red')
  plt.plot(x_values, y_parabola, label='Parabola of Best Fit', color='blue')
  plt.grid()
  plt.show()
  return None
create_plots2()

# Replace the value of 0 with your prediction of the satellite's position at t=60.

pred2=3132.72886747

"""**Problem 7**"""

# Replace the values of 0 with the NumPy arrays from Problem 7.
r_values = np.array([0.389, 0.724, 1, 1.524, 5.2, 9.51])
T_values = np.array([87.77, 224.7, 365.25, 686.95, 4332.62, 10759.2])
ln_r = np.log(r_values)
ln_T = np.log(T_values)
X3 = np.column_stack((np.ones(len(ln_T)), ln_T))

Y3 = ln_r

# Replace the values of 0 with the normal equation coefficient matrix and normal equation right-hand side from Problem 7.
transX3 = np.transpose(X3)
normal_coef3 = np.matmul(transX3, X3)

normal_vect3 = np.matmul(transX3, Y3)

# Replace the value of 0 with the least squares solution from Problem 7.

beta3=np.linalg.solve(normal_coef3,normal_vect3)

"""**Problem 8**"""

# Replace the values of 0 with your predictions for the semi-major axes of Uranus and Neptune.
period_Uranus = 30687.15
period_Neptune = 60190.03
ln_c = beta3[0]
a = beta3[1]
pred_Uran = np.exp(ln_c) * (period_Uranus ** a)
pred_Nept = np.exp(ln_c) * (period_Neptune ** a)

x10

"""**STOP!  BEFORE YOU SUBMIT THIS LAB:**  Go to the "Runtime" menu at the top of this page, and select "Restart and run all".  If any of the cells produce error messages, you will either need to fix the error(s) or delete the code that is causing the error(s).  Then use "Restart and run all" again to see if there are any new errors.  Repeat this until no new error messages show up.

**You are not ready to submit until you are able to select "Restart and run all" without any new error messages showing up.  Your code will not be able to be graded if there are any error messages.**

To submit your lab for grading you must first download it to your compute as .py file. In the "File" menu select "Download .py". The resulting file can then be uploaded to http://www.math.byu.edu:30000 for grading.
"""