# -*- coding: utf-8 -*-
"""LUDecomp_GaussianElimination.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1QHR0MHMcrCpUZGoid0Sb09934RavzcXw

#**Lab 5 - LU decompositions and Gaussian elimination**

Enter your code in the spaces provided. Do not change any of the variable names or function names that are already provided for you. In places where we specify the name of the return value, make sure that your code produces the a value with the correct name.
"""

# Do not edit this cell.

LabID="Lab5"

try:
  from graderHelp import ISGRADEPLOT
except ImportError:
  ISGRADEPLOT = True

"""**Enter your name, section number, and BYU NetID**"""

# Enter your first and last names in between the quotation marks.

first_name="Miaja"

last_name="Coombs"

# Enter your Math 215 section number in between the quotation marks.

section_number="001"

# Enter your BYU NetID in between the quotation marks.  NOT YOUR BYU ID NUMBER!

BYUNetID="miajac"

"""**Import NumPy**"""

import numpy as np

"""**Problem 1**"""

def augment(A,b):
  mA=A.copy()
  mAb=np.column_stack((mA,b))
  return mAb
C=np.array([[3,2,1,1],[1,-2,1,1],[5,0,1,5]])
b=np.array([-1,3,2])
augment(C,b)

"""**Problem 2**"""

def first_column_zeros(A):
  B=np.copy(A)
  first = B[0, 0]
  for i in range(1, B.shape[0]):
    B[i, :] = B[i, :] - (B[i,0]/first) * B[0, :]
  return B
A=np.array([[2,1,3,1],[1,2,-1,2.5],[4,2,-1,1]])
first_column_zeros(A)

"""**Problem 3**"""

def row_echelon(A,b):
  mA=A.copy()
  mAb=np.column_stack((mA,b))
  num_rows, num_cols = mAb.shape
  pivot_row = 0
  for col in range(num_cols):
    for i in range(pivot_row + 1, num_rows):
      mAb[i] -= (mAb[i, col] / mAb[pivot_row, col]) * mAb[pivot_row]
    pivot_row += 1
    if pivot_row >= num_rows or col >= num_cols:
      break
  return mAb
A=np.array([[3,1,-2],[1,2,-5],[2,-4,1]])
b=np.array([1.1,2,-3])
row_echelon(A,b)

"""**Problem 4**"""

from IPython.utils.path import ensure_dir_exists
def LU_decomposition(A):
  U=np.copy(A)
  U = np.copy(A)
  m, n = U.shape
  L = np.identity(m)
  for j in range(n-1):
    for i in range(j+1, m):
      L[i, j] = U[i, j] / U[j, j]
      U[i, :] = U[i, :] - L[i, j] * U[j, :]
  return L, U
A=np.array([[3,1,-2],[1.5,2,-5],[2,-4,1]])
LU_decomposition(A)

"""**Problem 5**"""

def forward_substitution(L,b): # Accepts a lower triangular square matrix L and a vector b, solves Ly=b for y.
  n = len(b)
  y = np.zeros(n)
  for i in range(n):
    y[i] = b[i] - np.dot(y[:i], L[i, :i])
    y[i] /= L[i, i]
  return y
L=np.array([[1,0,0],[3,1,0],[-1.1,2,1]])
b=np.array([-2.1,1,-1])
forward_substitution(L,b)

"""**Problem 6**"""

def back_substitution(U,y):    # Accepts an upper triangular square matrix U and a vector b, solves Ux=b for x.
  n = len(y)
  x = np.zeros(n)
  for i in range(n-1, -1, -1):
    sum_term = np.sum(U[i, i+1:] * x[i+1:])
    x[i] = (y[i] - sum_term) / U[i, i]
  return x

U = np.array([[2, -3.1, 1],[0, 1, 3],[0, 0, 4]])
y = np.array([1, -2.1, 3])
back_substitution(U,y)

"""**Problem 7**"""

def LU_solver(A,b):
  L,U=LU_decomposition(A)
  fw=forward_substitution(L,b)
  bw=back_substitution(U,fw)
  return bw
A=np.array([[3,1,-2],[1.5,2,-5],[2,-4,1]])
b=np.array([1.1,3,-2])
LU_solver(A,b)

"""**STOP!  BEFORE YOU SUBMIT THIS LAB:**  Go to the "Runtime" menu at the top of this page, and select "Restart and run all".  If any of the cells produce error messages, you will either need to fix the error(s) or delete the code that is causing the error(s).  Then use "Restart and run all" again to see if there are any new errors.  Repeat this until no new error messages show up.

**You are not ready to submit until you are able to select "Restart and run all" without any new error messages showing up.  Your code will not be able to be graded if there are any error messages.**

To submit your lab for grading you must first download it to your compute as .py file. In the "File" menu select "Download .py". The resulting file can then be uploaded to http://www.math.byu.edu:30000 for grading.
"""